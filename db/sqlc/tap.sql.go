// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.27.0
// source: tap.sql

package db

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const createTag = `-- name: CreateTag :one
INSERT INTO TAG (
    POST_ID,
    TAG_COMMENTS
) VALUES (
    $1,
    $2
) RETURNING post_id, tag_comments
`

type CreateTagParams struct {
	PostID      uuid.UUID `json:"post_id"`
	TagComments []string  `json:"tag_comments"`
}

func (q *Queries) CreateTag(ctx context.Context, arg CreateTagParams) (Tag, error) {
	row := q.db.QueryRow(ctx, createTag, arg.PostID, arg.TagComments)
	var i Tag
	err := row.Scan(&i.PostID, &i.TagComments)
	return i, err
}

const getTag = `-- name: GetTag :many
SELECT
    post_id, tag_comments
FROM
    TAG
WHERE
    TAG_COMMENTS LIKE '%'
                      || $1
                      || '%'
`

func (q *Queries) GetTag(ctx context.Context, dollar_1 pgtype.Text) ([]Tag, error) {
	rows, err := q.db.Query(ctx, getTag, dollar_1)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Tag{}
	for rows.Next() {
		var i Tag
		if err := rows.Scan(&i.PostID, &i.TagComments); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
